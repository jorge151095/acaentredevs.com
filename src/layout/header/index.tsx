import React from 'react';
import { NavBar } from '../../shared/components/navbar/navbar';

const Header = () => {
    return (
        <div>
            ICON
            <NavBar />
        </div>
    );
};

export default Header;
